/target
**/*.rs.bk
Cargo.lock.bak

# Local dev secrets / overrides
config/config.local.toml
.env
.env.local

# sqlx offline cache is committed; nothing to ignore.

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Node (e2e)
node_modules/

# Working dir used by `e2e/playwright.screenshots.config.ts` (the screenshots
# capture tool spins up the server with `config/screenshots.toml` and writes
# its sqlite + data files here).
screenshots-work/

.superpowers/
